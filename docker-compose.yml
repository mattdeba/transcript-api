version: "3.9"

services:
  nginx-proxy:
    image: nginxproxy/nginx-proxy:latest
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./certs:/etc/nginx/certs:rw
      - ./vhost.d:/etc/nginx/vhost.d
      - ./html:/usr/share/nginx/html
    labels:
      - com.github.nginx-proxy.nginx=true
    restart: unless-stopped
    networks:
      - proxy

  acme-companion:
    image: nginxproxy/acme-companion:latest
    container_name: acme-companion
    environment:
      - DEFAULT_EMAIL=matthieu.debarge@gmail.com
      - NGINX_PROXY_CONTAINER=nginx-proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./certs:/etc/nginx/certs:rw
      - ./vhost.d:/etc/nginx/vhost.d
      - ./html:/usr/share/nginx/html
      - ./acme:/etc/acme.sh
    depends_on:
      - nginx-proxy
    restart: unless-stopped
    networks:
      - proxy

  backend:
    build: https://github.com/mattdeba/weight-tracker-api.git
    container_name: nestjs-back
    expose:
      - "3000"  # vu par nginx-proxy, pas exposé à l'extérieur
    environment:
      # config DB (comme dans ton premier fichier)
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_NAME: myapp

      # config nginx-proxy / Let's Encrypt
      VIRTUAL_HOST: dockbarge.cloud        # ou api.dockbarge.cloud si tu préfères un sous-domaine
      VIRTUAL_PORT: 3000
      LETSENCRYPT_HOST: dockbarge.cloud
    depends_on:
      - db
    restart: unless-stopped
    networks:
      - proxy
      - app

  db:
    image: postgres:15-alpine
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: myapp
    volumes:
      - db_data:/var/lib/postgresql/data
    # pas besoin d'exposer le port vers l'extérieur
    # ports:
    #   - "5432:5432"
    networks:
      - app

networks:
  proxy:
  app:

volumes:
  db_data:
